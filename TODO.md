# TODO: Fix TelescopeController errors and verify

1. Remove incorrect import from TelescopeController.java
2. Run `./gradlew build` to verify no compilation errors
3. Run `./gradlew bootRun` to start application  
4. Test `/space/image` endpoint
